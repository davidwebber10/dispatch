node_modules/
dist/
coverage/
*.tsbuildinfo
.DS_Store
*.log
*.db
.env
.env.*
.vscode/
.idea/
.worktrees/
# Claude Code's native worktree isolation checks out here (embedded repos);
# unignored, the directory shows as untracked and trips the release clean-tree guard.
.claude/worktrees/

# Runtime data + tool scratch (never tracked)
.dispatch/
.playwright-mcp/

# Generated codex app-server bindings (regenerated at build time; not committed)
packages/core/.codex-appserver/

# Agent scratch: SDD ledgers and per-task reports (never ship to main)
.superpowers/
